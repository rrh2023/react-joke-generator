import React, {useState} from 'react'

const Reveal = ({punchline}) => {
  
    return (

        <div>
            <h3>Punchline:</h3>
            <p>{punchline}</p>
        </div>

    )
}

export default Reveal